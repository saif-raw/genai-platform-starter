Intialize Project
cd genai-platform-starter/deployment
mkdir cdk
cd cdk
cdk init app --language typescript


# Go to deployment/cdk
npm install
npm run build
cdk bootstrap
cdk deploy
cdk deploy --require-approval never


# Frontend
cd frontend\genai-pro-dashboard
npm install
# Add .env with VITE_API_BASE="https://oog4ijyedb.execute-api.us-east-1.amazonaws.com/prod"
npm run dev

#Troubleshoot
aws cloudformation describe-stack-events --stack-name CdkStack --region us-east-1


#Run this in the powershell
$body = @{
    prompt = "Who are you"
    provider = "groq"
    model = "openai/gpt-oss-20b"
}

$uri = "https://83jm2o790m.execute-api.us-east-1.amazonaws.com/prod/v1/hello"

Invoke-RestMethod -Uri $uri -Method Post -ContentType "application/json; charset=utf-8" -Body ($body | ConvertTo-Json)